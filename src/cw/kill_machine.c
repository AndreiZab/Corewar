#include "ft_cw.h"

void	ft_kill_machine(t_corewar *cw, int *play)
{
	// t_carriage	*carr;

	// carr = cw->carriages;
	// while (carr)
	// {
	// 	if (carr->cycle_live == 0)
	// 		ft_carr_kill(cw, carr);

	// 	carr = carr->next;
	// }
}